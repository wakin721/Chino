name: chino
description: Material 3 YOLO dataset annotation desktop application.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  file_picker: ^10.3.0
  path: ^1.9.1
  yaml: ^3.1.3
  uuid: ^4.5.1
  image: ^4.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
